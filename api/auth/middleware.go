package auth

import (
	"github.com/easyctf/openctf/structs"
	"gopkg.in/macaron.v1"
)

// RegisterUser is an API handler that allows users to register
func RegisterUser(c *macaron.Context, w *structs.Webserver) {
	// do nothing for now lol
}
